# myVPS — VPS 初始化脚本集

面向 Debian/Ubuntu 新 VPS 的基础安全初始化脚本集的领域语言。每个脚本自包含、幂等，由人工按 checklist 顺序执行，不经任何总控脚本调度。

## Language

**自包含脚本**:
单文件即可被 raw 拉取并独立执行的脚本；不 source 仓库内其他文件，公共函数内联于每个脚本且内容完全一致。
_Avoid_: 模块化脚本、入口脚本、子模块

**编排器**:
被本项目明确拒绝的形态：由一个总控脚本（如 `init.sh run`）按状态调度其余脚本。本项目中编排由人完成（README 提供命令，checklist 提供顺序）。决策记录见 `docs/adr/0001-no-orchestrator.md`。
_Avoid_: runner、入口脚本

**闸门**:
脚本内强制的人工确认点（y/N 交互）。对应三条铁律的生死关口；答 N 即退出（退出码 1），不存在"跳过"。
_Avoid_: 确认弹窗、跳过选项

**前置自检**:
脚本启动时对系统真实状态的检查（ufw 是否放行端口、用户是否存在）。不满足即拒绝执行。检查的是当下系统状态，不是脚本自己的历史记录。
_Avoid_: 状态检查文件、进度记录

**配置文件**（`/root/.vps-init.conf`）:
跨脚本参数在目标机器上的本地存储（端口、用户名、公钥等）。脚本读不到对应键则交互询问并写盘；600 权限、root 属主，永不进入仓库。
_Avoid_: init.conf、设置向导

**备份目录**（`/root/vps-init-backups/<时间戳>/`）:
脚本修改任何系统配置文件前自动留存的副本。恢复靠人工从副本还原，脚本不做自动回滚。

**终检报告**（`/root/vps-init-report.md`）:
`verify.sh` 产出的绿/红检查报告，兼作初始化参数的归档记录。有红项时 `verify.sh` 以退出码 1 结束。
